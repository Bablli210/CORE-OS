// The web manifest and the browser theme colour need literal colours: these are the values of --background (neutral-0)
// and --primary (neutral-900) in src/styles/tokens.css. Change them together when branding lands.
export const PWA_BACKGROUND = "#ffffff"; // eslint-disable-line no-restricted-syntax
export const PWA_THEME = "#171717"; // eslint-disable-line no-restricted-syntax
